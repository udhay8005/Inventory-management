<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_wms_rack_generator_form" model="ir.ui.view">
            <field name="name">wms.rack.generator.form</field>
            <field name="model">wms.rack.generator</field>
            <field name="arch" type="xml">
                <form string="Generate Rack">
                    <group>
                        <field name="warehouse_id"/>
                        <field name="parent_location_id"/>
                        <field name="rack_code"/>
                        <field name="dividers_per_level"/>
                        <field name="capacity_per_slot"/>
                        <field name="level_prefix"/>
                        <field name="divider_prefix"/>
                        <field name="slot_prefix"/>
                    </group>
                    <footer>
                        <button string="Generate" name="action_generate" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_wms_rack_generator" model="ir.actions.act_window">
            <field name="name">Generate Rack</field>
            <field name="res_model">wms.rack.generator</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>
